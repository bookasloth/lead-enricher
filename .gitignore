# ── Data / PII — NEVER commit (real people's names, emails, phones) ──
*.csv
*.ndjson
*.json
*.db
*.db-shm
*.db-wal
*.log
*.err
*.html
lists/
node_modules/

# keep the project manifest (overrides *.json above)
!package.json
!package-lock.json

# keep source config + test fixtures (override *.json above); these are code, not data
!gmaps-scoring.json
!gmaps/fixtures/
!gmaps/fixtures/*.json

# keep the UI source (override *.html above); it's code, not scraped data
!public/index.html

watchdog.out
watchdog.err
watchdog.stop

# python laya service
services/laya/.venv/
__pycache__/
*.pyc
